from setuptools import setup

# Configuration is primarily in pyproject.toml (and potentially setup.cfg)
# This minimal setup.py helps some tools find the build backend and package.
if __name__ == "__main__":
    setup()
